/**
 * photovoltaic-information application module.
 * <p>
 * Packaged by business context (not only by technical layer):
 * <ul>
 *   <li>{@code station} — station archive, price, contract, screen, statistics, dictionary</li>
 *   <li>{@code ops} — inspection &amp; work orders</li>
 *   <li>{@code monitor} — thresholds &amp; realtime/history monitor (ports/adapters)</li>
 *   <li>{@code report} — daily power report &amp; jobs</li>
 *   <li>{@code device} — device &amp; factory master data</li>
 *   <li>{@code identity} — auth, users, security filters</li>
 *   <li>{@code query} — exception search, experience</li>
 *   <li>{@code shared} — cross-cutting config, aspects, API envelope</li>
 *   <li>{@code infrastructure} — minio/local upload, (mappers stay under {@code mapper})</li>
 *   <li>{@code dto}/{@code mapper} — shared API models &amp; persistence for now</li>
 * </ul>
 */
package qrsoft.information;
